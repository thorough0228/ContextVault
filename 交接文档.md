# ContextVault 对话交接文档

> 交接时间:2026-09-18。供新会话 / 新开发者接手使用。
> 排错先查 [problems/](problems/README.md) 案例库(11 个真实案例),本文档讲全局状态。

## 1. 项目是什么

**ContextVault** — 多租户 RAG-as-a-Service 平台(改名史:rag_saas → mind_harbor → contextvault,
2026-09-18 定名,全量改名已完成,grep 零残留)。

严格的 `User → RAG → Document → Chunk` 归属链、pgvector 语义检索、流式 LLM 对话 + 引用、
多格式文件解析(pdf/txt/csv/json/jsonl)、本地离线 embedding。

## 2. Monorepo 结构与启动

| 目录 | 技术 | 启动 |
|---|---|---|
| `apps/api` | FastAPI + SQLAlchemy(async) + Celery,端口 8000 | `python -m uvicorn app.main:app --reload --port 8000` |
| `apps/worker` | Celery(solo 池,Windows 自动强制) | `python -m celery -A app.celery_app worker --loglevel=INFO` |
| `apps/web` | Next.js 14 + Tailwind,端口 3000 | `npm run dev` |
| `evals/` | RAG 质量测评体系(检索指标自建 + DeepEval judge,详见其 README) | `cd evals && python -m pytest evals/suites -m offline` |
| `packages/shared` | Python(`contextvault_shared`)+ TS(`@contextvault/shared`),两侧手工同步 | — |
| `infrastructure/` | docker-compose(全栈 7 服务) | `docker compose -f infrastructure/docker-compose.yml --env-file .env up --build` |
| `scripts/` | 运维工具 | 见 §7 |
| `docs/problems/` | 11 个真实踩坑案例(现象/根因/解决) | 排错先看这里 |

**Python 环境**:conda env `agents`(`C:/Users/Thorough/.conda/envs/agents/python.exe`)。
**前端**:npm(Node 20)。**测试**:apps/api 183 / apps/worker 7 / 根 smoke 8,全绿(2026-09-18)。

## 3. 基础设施(容器,已按新命名重建)

| 容器 | 镜像 | 说明 |
|---|---|---|
| `contextvault-pg` | pgvector/pgvector:pg16(**必须用这个**,裸 Postgres 无 vector 扩展) | 用户/库 `contextvault`,密码 `change-me-locally` |
| `contextvault-redis` | redis:7-alpine | broker db1 / result db2 |
| `contextvault-minio` | minio(minioadmin / change-me-locally) | 桶 `contextvault` |

一键重建(数据会丢,先 dump):
```bash
docker exec contextvault-pg pg_dump -U contextvault --no-owner --no-privileges contextvault > dump.sql
docker rm -f contextvault-pg contextvault-redis contextvault-minio
docker run -d --name contextvault-pg -p 5432:5432 -e POSTGRES_USER=contextvault -e POSTGRES_PASSWORD=change-me-locally -e POSTGRES_DB=contextvault pgvector/pgvector:pg16
# redis/minio 同理,见 README;然后 docker exec -i contextvault-pg psql -U contextvault -d contextvault < dump.sql
```

## 4. AI Provider 现状

- **LLM = MiniMax-M3**(OpenAI 兼容,`https://api.minimaxi.com/v1`,Key 已配置在 `.env` 的
  `LLM_OPENAI_API_KEY`,已实测可用;`LLM_STRIP_THINK=true` 过滤推理过程;
  `LLM_TIMEOUT_SECONDS=120`;`LLM_MAX_CONTEXT_TOKENS=8192` — 注意这是**单次回答输出上限**,
  MiniMax 上限 524288,别设更大)。
- **Embedding = 硅基流动 API `BAAI/bge-m3`**(1024 维多语模型,OpenAI 兼容
  `https://api.siliconflow.cn/v1`,**免费模型**;`EMBEDDING_PROVIDER=openai`)。
  2026-09-19 由本地 bge-small-zh 升级,golden 检索 hit@5 从 0.29 → 0.86-0.93,
  见 `evals/baseline.json`。本地 `models/bge-m3/` 文件保留但已不使用
  (4GB GPU 在 pytest 进程里会 segfault,故弃本地走 API)。
- **硅基流动专有配置**:`EMBEDDING_OPENAI_SEND_DIMENSIONS=0`(bge-m3 拒绝 dimensions
  参数,输出即原生 1024)+ `EMBEDDING_OPENAI_REQUEST_INTERVAL=0.6`
  (**免费档限流表现为 400/20015 而非 429**,批间必须留间隔;
  403/超时可调 `EMBEDDING_TIMEOUT_SECONDS=120` + `EMBEDDING_BATCH_SIZE=8`)。
- **切块窗口 = 500/100**(`.env` 的 `CHUNK_SIZE_CHARS/CHUNK_OVERLAP_CHARS`;
  原 1500/200 语义稀释,evals 驱动下调)。**改切块/模型后必须
  `alembic upgrade head`(若换维度)+ `python scripts/requeue_documents.py --all` 重建向量**。
  超大文档 worker 卡死时的兜底:`python scripts/backfill_embeddings.py <document_id>`
  (直连 API 逐批回填,断点可续跑)。
- **混合检索已启用**(`.env` 的 `SEARCH_MODE=hybrid`):向量 + 关键词
  (`document_chunks.tokenized` 列,jieba 分词,GIN tsvector 索引,迁移 0012)
  RRF 融合(k=60),再经硅基流动 `BAAI/bge-reranker-v2-m3` 精排
  (免费、同 key;失败自动降级为融合排序)。回退纯向量:设 `SEARCH_MODE=vector`。
- **对话记忆已启用**:窗口外旧轮次自动压缩为滚动摘要(`conversations.summary`);
  每轮结束后异步抽取长期事实(`memory_facts` 表,任务 `app.tasks.extract_memory`)
  并在后续对话注入(顺序:context → memories → summary → history)。
  关闭:`MEMORY_EXTRACTION_ENABLED=0` / `MEMORY_INJECTION_ENABLED=0`。
- **文档更新已支持**(`.env` 的 `DOCUMENT_UPDATE_STRATEGY`,迁移 0013):
  - 上传即算 `content_hash`(SHA-256);同 RAG 同名重复上传返回 **409**(提示走更新)。
  - `PUT /api/v1/documents/{id}/content?strategy=replace|bluegreen`:
    hash 相同 → 200 幂等跳过;`replace` → 同文档先删后增重投
    (失败自动保留旧数据);`bluegreen` → 新文档独立入库,READY 后自动
    `superseded_by` 指针切换(检索立即只见新版,`POST .../rollback` 可回滚)。
  - 检索全路径(向量/关键词×PG/SQLite)自动过滤 superseded 文档。
  - 前端 Documents 面板每文档有 Update 按钮。
- 备选(字段都保留在 .env):智谱 embedding-3(OpenAI 兼容,Key 已填但账户欠费)、
  MiniMax embo-01(专用 provider,需 GroupId)、任何 OpenAI 兼容端点、
  旧 bge-small-zh-v1.5(仍在 `models/`,512 维,需配套对齐迁移回退)。
- **SYSTEM_PROMPT 已加固注入防御**(chat_service.py:检索上下文=不可信数据规则 +
  上下文 header 加框架句),robustness 评测金丝雀泄漏 8/8→0。加固时注意:
  `hash_provider.py` 靠 `"Retrieved context"`/`"(no context"` 两个子串定位上下文块,
  改 prompt 措辞时不能破坏它们。

## 5. 数据现状(2026-09-19)

- **25 个文档:19 READY + 6 FAILED**;向量 10244+ 维 bge-m3,共 **10591 条**,
  `alembic_version` = **0011**(vector(1024))。切块窗口 500/100
  (chunk 总量比 1500 窗口时代多 ~2.9 倍,检索更细)。
- **6 篇论文 FAILED**(itransformer / FEDformer / patchTST / TimeMosaic / OLinear /
  AGCNDE):原件丢失,且 bge-m3 迁移(0011)清空了旧向量——**重传 PDF 后运行
  `python scripts/requeue_documents.py` 即恢复**,无需其他操作。
- GPU 用户注意:`.env` 已开 `EMBEDDING_LOCAL_FP16=1`(4GB 显存跑 bge-m3 的推荐配置,
  显存减半速度 ~4-5×;自动跳过 CPU/老 GPU)+ `EMBEDDING_BATCH_SIZE=16`。
- 旧容器/测试账号/测试数据已全部清理,库里只剩用户 `2729174547@qq.com` 的数据
  (+ evals live profile 的临时测评 RAG,套件运行后自动删除)。

## 6. 本轮会话完成的大事(细节见 problems/ 案例库)

1. rag_saas → mind_harbor → contextvault 两次全量改名(含 DB/桶/包名,数据 dump/restore 保全)。
2. 修复架构级 bug(全部已修复并写档):`.env` 从未被加载(env_file 相对路径)、
   worker 任务从未注册(双 `app` 包遮蔽,`__path__` 复合包方案)、迁移四类 bug
   (ENUM 重复/转型/id 超长/布尔默认值)、Windows celery prefork 必崩(强制 solo)、
   pgvector HNSW 2000 维上限、PDF NUL 字节、ORM/检索的 vector 绑定。
3. 新功能:csv/json/jsonl 解析、多文件选择排队上传、chat Markdown 渲染、
   本地 embedding provider、`<think>` 流式过滤、401 自动跳登录 + 7 天 token、
   大文件上传(1GiB 上限 + 磁盘 spool,内存平坦)。

## 7. 常用运维命令

```bash
# 重投卡住/失败的文档(换 embedding 或 worker 停过之后)
python scripts/requeue_documents.py [--all]

# 大 CSV 采样裁剪(Spotify 1.1GB 这类)
python scripts/trim_csv.py input.csv -o sample.csv --rows 50000

# 队列积压检查(注意 broker 在 db1)
docker exec contextvault-redis redis-cli -n 1 LLEN default
```

## 8. 已知问题 / 待办

1. **6 篇论文 PDF 原件待重传**(见 §5),重传后无需其他操作。
2. ~~**本地磁盘根文件夹仍叫 `mind_harbor`**~~ **已改名 contextvault → 实际为 `context_vault`**(2026-09-19 确认),
   `.env` 的 `EMBEDDING_MODEL` 路径已同步修正为 `.../context_vault/models/bge-small-zh-v1.5`。
3. **前端没有测试基建**(无 vitest/jest),前端改动靠 typecheck + build + 手动验证。
4. Phase 3 规划:httpOnly cookie + refresh token(当前 7 天 JWT + 401 自动跳登录)。
5. 超大文件(>100MB)的 worker 流式解析未做——当前 parse 全量进内存,
   数百 MB 文件可跑(已验证 60MB),GB 级需流式重构。
6. 本会话的 API(8000)/worker 后台实例随会话结束停止,**用户需在自己的终端启动**(§2)。
7. **[测评发现·高优] 注入抵抗不足 — 已修复(2026-09-19)**:SYSTEM_PROMPT 加固
   (上下文=不可信数据规则)+ 上下文 header 框架句后,robustness 评测金丝雀泄漏
   8/8 → 0,详见 `evals/baseline.json` 的 robustness 段。残留:金丝雀标记在
   防御性解释中被复述不算泄漏(套件已按语境豁免)。
8. **[测评发现·中优] 检索质量偏低 — 已修复(2026-09-19)**:换 bge-m3(多语 1024 维)
   + 切块 1500/200 → 500/100 后,golden 检索 hit@5 0.29 → 0.90、hit@10 → 0.95,
   详见 `evals/baseline.json`。注意:换模型需要 CPU 重建向量(~3700 chunks 约 1 小时)。
9. **6 篇时序论文因丢失原件在 bge-m3 迁移中失去向量**(itransformer/FEDformer/
   patchTST/TimeMosaic/OLinear/AGCNDE,用户已接受):重传 PDF 后运行
   `python scripts/requeue_documents.py` 即自动恢复(不再需要回退旧模型)。

## 9. 验证清单(接手后先跑一遍)

```bash
cd apps/api && python -m pytest          # 185 passed
cd apps/worker && python -m pytest       # 7 passed
python -m pytest tests/                  # 9 passed(根目录,含 evals 离线套件编排)
cd evals && python -m pytest evals/suites -m offline   # 测评离线套件(检索/契约/健壮性机制)
cd evals && python -m pytest evals/suites -m online    # 在线测评(judge,花 token)
EVAL_PROFILE=live python -m pytest evals/suites/test_retrieval.py -m offline  # live 检索
curl http://localhost:8000/api/v1/health # postgres/redis ok
```
UI:登录 → 乒乓球 RAG → 搜「乒乓球胶皮怎么选」应命中 chunks.json → chat 回答带引用芯片。
